# Test training configuration for cloud tests
training:
  max_epochs: 1
  batch_size: 32
  accelerator: gpu
  devices: 1
  strategy: auto

data:
  data_path: s3://test-bucket/datasets/test-data.h5

model:
  name: SimpleNet
  hidden_size: 64

logging:
  project_name: test-project
  experiment_name: test-experiment

